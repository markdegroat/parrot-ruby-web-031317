# Create method `parrot` that outputs a given phrase and
# returns the phrase
def parrot(phrase = "Squawk!")
  #because this returns nil we need to explicitly return
  puts phrase
  return phrase
end
